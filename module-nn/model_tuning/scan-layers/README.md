`scan-layers`: Varying the number of hidden layers in the model
===============================================================

This directory will contain model tuning results obtained by
varying the hidden layer and neuron configurations.
The tuning will be done in a Jupyter notebook.

